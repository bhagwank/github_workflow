name: produce

on:
  workflow_dispatch:
  push:
    branches:
      - master
    tags:
      - '*'
  schedule:
    - cron: '45 08 * * *'
    
jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Check out the code
      uses: actions/checkout@v2
    - name: wait
      run: sleep 15
